import Image from "next/image";
import Link from "next/link";
import { brand } from "@/lib/brand";

const quickLinks = [["Submit a challenge","/submit-problem"],["Browse challenges","/problem-statements"],["Join as member","/join"],["About NumSum","/about"]] as const;
const resourceLinks = [["Sitemap","/"],["Contact","/about#contact"],["Privacy","/about#privacy"],["Terms","/about#terms"]] as const;
export function SiteFooter() { return <footer className="border-t border-white/10 bg-[#02050a] px-6 py-12 text-sm text-white/60"><div className="mx-auto grid max-w-7xl gap-8 md:grid-cols-[1.2fr_.7fr_.7fr]"><div className="flex items-start gap-3"><Image src={brand.logo} alt="NumSum Labs logo" width={46} height={46} className="rounded-xl bg-white p-1" /><div><p className="font-display text-xl font-bold text-white">NumSum Labs</p><p className="mt-2 max-w-md text-blue-100/80">MSME-first industrial innovation and upgradation ecosystem that turns practical challenges into structured knowledge, SOPs, innovation challenges, pilots, and validated impact.</p><p className="mt-4">© 2026 NumSum Labs. All Rights Reserved.</p></div></div><div><p className="font-semibold text-white">Quick links</p><div className="mt-3 grid gap-2">{quickLinks.map(([label,href])=><Link key={href} href={href} className="hover:text-white">{label}</Link>)}</div></div><div><p className="font-semibold text-white">Legal & contact</p><div className="mt-3 grid gap-2">{resourceLinks.map(([label,href])=><Link key={href} href={href} className="hover:text-white">{label}</Link>)}</div><p className="mt-4 text-white/45">Public pages show only approved, public-safe records.</p></div></div></footer>; }
