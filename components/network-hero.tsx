"use client";
import { motion } from "framer-motion";
const nodes = ["Industry","Research","Innovation","Technology","Talent","Engineering"];
export function NetworkHero(){return <div className="relative mx-auto h-[420px] max-w-4xl"><svg className="absolute inset-0 h-full w-full opacity-70">{nodes.map((_,i)=>nodes.slice(i+1).map((_,j)=><motion.line key={`${i}-${j}`} x1={`${18+i*13}%`} y1={`${30+(i%3)*22}%`} x2={`${18+(i+j+1)*13}%`} y2={`${30+((i+j+1)%3)*22}%`} stroke="rgba(47,128,255,.38)" strokeWidth="1" initial={{pathLength:0}} animate={{pathLength:1}} transition={{duration:2,repeat:Infinity,repeatType:"reverse",delay:i*.15}}/>))}</svg>{nodes.map((node,i)=><motion.div key={node} className="glass absolute rounded-2xl px-5 py-3 font-display text-sm shadow-glow" style={{left:`${10+i*14}%`,top:`${22+(i%3)*25}%`}} animate={{y:[0,-12,0],opacity:[.75,1,.75]}} transition={{duration:4,repeat:Infinity,delay:i*.25}}>{node}</motion.div>)}</div>}
