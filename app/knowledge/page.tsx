"use client";import { useEffect,useState } from "react";import { EmptyState, ErrorState, LoadingState } from "@/components/data-states";import { Card } from "@/components/ui";import { getLabelItems } from "@/lib/repositories/firestore";import type { LabelItem } from "@/lib/types";
export default function Knowledge(){const [items,setItems]=useState<LabelItem[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);useEffect(()=>{getLabelItems("knowledge_categories").then(setItems).catch(()=>setError("Unable to load knowledge categories from Firestore.")).finally(()=>setLoading(false));},[]);return <main className="min-h-screen bg-navy px-6 py-10"><h1 className="font-display text-5xl">Knowledge Repository</h1>{error&&<div className="mt-6"><ErrorState message={error}/></div>}<div className="mt-8 grid gap-4 md:grid-cols-3">{loading?<LoadingState label="Loading knowledge categories"/>:items.length?items.map(x=><Card key={x.id}>{x.name}<p className="mt-3 text-white/60">{x.description || "Search, tag, bookmark and download public or private assets."}</p></Card>):<EmptyState message="No knowledge categories are configured yet."/>}</div></main>}
