"use client";
import { useEffect, useState } from "react"; import { AuthGate } from "@/components/auth-gate"; import { useAuth } from "@/components/auth-provider"; import { Card } from "@/components/ui"; import { getMyCompetitionSubmissions } from "@/lib/repositories/firestore";
export default function MySubmissions(){const{user}=useAuth();const[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(user)getMyCompetitionSubmissions(user.uid).then(setRows)},[user]);return <AuthGate><main className="min-h-screen bg-navy px-6 py-10"><h1 className="font-display text-5xl">My Submissions</h1><Card className="mt-8"><p className="text-white/60">Drafts can be edited before final submission/deadline; final submissions are read-only for members.</p><pre className="mt-4 text-xs text-white/70">{JSON.stringify(rows,null,2)}</pre></Card></main></AuthGate>}
