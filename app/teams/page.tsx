"use client";import { useEffect,useState } from "react";import { EmptyState, ErrorState, LoadingState } from "@/components/data-states";import { Card } from "@/components/ui";import { getLabelItems } from "@/lib/repositories/firestore";import type { LabelItem } from "@/lib/types";
export default function Teams(){const [roles,setRoles]=useState<LabelItem[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);useEffect(()=>{getLabelItems("team_roles").then(setRoles).catch(()=>setError("Unable to load team roles from Firestore.")).finally(()=>setLoading(false));},[]);return <main className="min-h-screen bg-navy px-6 py-10"><h1 className="font-display text-5xl">Team Formation</h1>{error&&<div className="mt-6"><ErrorState message={error}/></div>}<Card className="mt-8"><p>Create teams, join teams, apply, invite members and manage roles.</p><div className="mt-4 flex flex-wrap gap-2">{loading?<LoadingState label="Loading team roles"/>:roles.length?roles.map(r=><span className="rounded-full bg-blue-500/20 px-3 py-1" key={r.id}>{r.name}</span>):<EmptyState message="No team roles are configured yet."/>}</div></Card></main>}
