import { Card } from "@/components/ui";
import { getAdminSOPDocuments } from "@/lib/repositories/firestore";
export default async function AdminSOPPage({ searchParams }: { searchParams?: Promise<Record<string,string>> }) { const sp = await searchParams; const q=(sp?.q||"").toLowerCase(); const rows=(await getAdminSOPDocuments()).filter((s)=>!q||s.title.toLowerCase().includes(q)||s.tags?.some(t=>t.toLowerCase().includes(q))); return <main className="min-h-screen bg-navy px-6 py-10"><h1 className="font-display text-5xl">Admin SOP Library</h1><p className="mt-3 text-white/60">Official procedures linked to problem statements.</p><form className="mt-6 grid gap-3 md:grid-cols-6"><input name="q" placeholder="Search title/tags" className="rounded-xl border border-white/10 bg-black/30 p-3 md:col-span-2"/><input name="status" placeholder="status" className="rounded-xl border border-white/10 bg-black/30 p-3"/><input name="visibility" placeholder="visibility" className="rounded-xl border border-white/10 bg-black/30 p-3"/><input name="processArea" placeholder="process area" className="rounded-xl border border-white/10 bg-black/30 p-3"/><button className="rounded-full bg-blue-400 px-5 py-3 font-semibold text-navy">Filter</button></form><Card className="mt-6 overflow-x-auto"><table className="min-w-full text-sm"><thead className="text-left text-white/45"><tr><th className="p-3">Title</th><th>Status</th><th>Visibility</th><th>Process</th><th>Version</th><th>Problem</th></tr></thead><tbody>{rows.map((s)=><tr key={s.id} className="border-t border-white/10"><td className="p-3">{s.title}<p className="text-xs text-white/45">{s.tags?.join(", ")}</p></td><td>{s.status}</td><td>{s.visibility}</td><td>{s.processArea}</td><td>{s.version}</td><td><a className="text-blue-200" href={`/problem-statements/${s.problemStatementId}`}>Workspace</a></td></tr>)}</tbody></table></Card></main> }
