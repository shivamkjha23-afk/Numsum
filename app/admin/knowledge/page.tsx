import { Card } from "@/components/ui";
import { getAdminKnowledgeAssets } from "@/lib/repositories/firestore";
export default async function AdminKnowledgePage({ searchParams }: { searchParams?: Promise<Record<string,string>> }) { const sp = await searchParams; const q=(sp?.q||"").toLowerCase(); const rows=(await getAdminKnowledgeAssets()).filter((a)=>!q||a.title.toLowerCase().includes(q)||a.tags?.some(t=>t.toLowerCase().includes(q))); return <main className="min-h-screen bg-navy px-6 py-10"><h1 className="font-display text-5xl">Admin Knowledge Assets</h1><p className="mt-3 text-white/60">Review, publish, and trace every asset back to its problem workspace.</p><form className="mt-6 grid gap-3 md:grid-cols-6"><input name="q" placeholder="Search title/tags" className="rounded-xl border border-white/10 bg-black/30 p-3 md:col-span-2"/><input name="status" placeholder="status" className="rounded-xl border border-white/10 bg-black/30 p-3"/><input name="visibility" placeholder="visibility" className="rounded-xl border border-white/10 bg-black/30 p-3"/><input name="category" placeholder="category" className="rounded-xl border border-white/10 bg-black/30 p-3"/><button className="rounded-full bg-blue-400 px-5 py-3 font-semibold text-navy">Filter</button></form><Card className="mt-6 overflow-x-auto"><table className="min-w-full text-sm"><thead className="text-left text-white/45"><tr><th className="p-3">Title</th><th>Status</th><th>Visibility</th><th>Category</th><th>Problem</th></tr></thead><tbody>{rows.map((a)=><tr key={a.id} className="border-t border-white/10"><td className="p-3">{a.title}<p className="text-xs text-white/45">{a.tags?.join(", ")}</p></td><td>{a.status}</td><td>{a.visibility}</td><td>{a.category}</td><td><a className="text-blue-200" href={`/problem-statements/${a.problemStatementId}`}>Workspace</a></td></tr>)}</tbody></table></Card></main> }
